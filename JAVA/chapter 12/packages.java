package krish;

public class packages {
    public static void main(String[] args) {
        System.out.println("hello world");
    }

}

// V.V.V IMP(spaces are very very imp in below comands whereever needed)

// javac -d. *.java (assign all the files in their respective folders)
// javac -d. packages.java (creates a package folder for only packages file)
// javac -d hello *.java (creates all the packages inside hello named folder)
